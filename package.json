{
  "name": "collage-endpoints",
  "version": "1.0.0",
  "description": "Simple backend to handle Spotify's WebAPI authentication",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gustavares",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "querystring": "^0.2.0",
    "request": "^2.83.0"
  }
}
